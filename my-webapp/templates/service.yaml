apiVersion: v1
kind: Service
metadata:
  name: my-gitops-webapp-service
  namespace: terraform-practice
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    # 🌟 測試環境先固定用 30088，下一關換成 Ingress 後我們就徹底解放這個破綻
    nodePort: 30088